server:
  port: 8012

# Making connection with database
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/banking_app
    username: bankuser
    password: ${db_pass}

#jpa and hibernate connections
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
